<link rel="stylesheet" href="{{ asset('bundles/cookbookcore/css/cookbook_green.css') }}" type="text/css" media="all" />

{% if error %}
    <div>{{ error.message }}</div>
{% endif %}
<div class="login_cookbook">
<div class="header_cookbook">
<div id="logo_cookbook"><a href="{{ path('home') }}"><img src="{{ asset('bundles/cookbookcore/images/logo_findacook.png') }}" border="0" /></a></div>
</div>
<div>
<form action="{{ path('login_check') }}" method="post">
    <label for="username">login</label>
    <input type="text" id="username" name="_username" value="{{ last_username }}" />
<br /><br />
    <label for="password">Mot de passe</label>
    <input type="password" id="password" name="_password" />

    {#
        If you want to control the URL the user is redirected to on success (more details below)
        <input type="hidden" name="_target_path" value="/account" />
    #}
    <br />
    <button type="submit">Se connecter</button>
</form>
</div></div>