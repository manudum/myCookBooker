<?php

namespace Cookbook\CoreBundle\Entity;

use Doctrine\ORM\EntityRepository;

/**
 * TypeRecipeRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class TypeRecipeRepository extends EntityRepository
{
    public function deleteTypeRecipe($id)
    {
        $q = $this->getEntityManager()
            ->createQueryBuilder()
            ->delete('Cookbook\CoreBundle\Entity\TypeRecipe', 'i')
            ->where('i.id = :ingredient_id')
            ->setParameter('ingredient_id', $id)
            ->getQuery()
        ;
        return 1 === $q->getScalarResult();
    }
}