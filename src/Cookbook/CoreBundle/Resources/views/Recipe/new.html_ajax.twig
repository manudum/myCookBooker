<form id="recipe_form" action="{{ path('recipe_new') }}" method="post" {{ form_enctype(form) }}>

{{ form_errors(form) }}
<table>
<tr>
<td>
    {{ form_label(form.name,'Nom du plat') }}
    {{ form_errors(form.name) }}
</td><td>
    {{ form_widget(form.name) }}
</td>
</tr>

<tr><td>
    {{ form_label(form.category, 'Catégorie') }}
    {{ form_errors(form.category) }}
</td><td>
    <div id="categories" class="inline">{{ form_widget(form.category) }}</div>
</td>
</tr>

</table>
{{ form_rest(form) }}

</form>