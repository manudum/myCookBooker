<form id="format_form" action="{% if action == 'formatrecipe_new' %}{{ path('formatrecipe_new') }}{% else %}
{{ path('formatrecipe_edit', { 'id': form.vars.value.id }) }} }}
{% endif %}" method="post" {{ form_enctype(form) }}>
   {{ form_errors(form) }}

<div>
    {{ form_label(form.name,'Nom') }}
    {{ form_errors(form.name) }}
    {{ form_widget(form.name) }}
</div>
{{ form_rest(form) }}
</form>