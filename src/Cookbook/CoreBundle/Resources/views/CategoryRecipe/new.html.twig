<form id="cat_form" action="{% if action == 'categoryrecipe_new' %}{{ path('categoryrecipe_new') }}{% else %}
{{ path('categoryrecipe_edit', { 'id': form.vars.value.id }) }} }}
{% endif %}" method="post" {{ form_enctype(form) }}>
   {{ form_errors(form) }}

<div>
    {{ form_label(form.name,'Nom') }}
    {{ form_errors(form.name) }}
    {{ form_widget(form.name) }}
</div>
{{ form_rest(form) }}
</form>
