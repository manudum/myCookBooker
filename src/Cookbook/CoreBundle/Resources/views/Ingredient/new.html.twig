<form id="ingredient_form" action="{{ path('ingredient_new') }}" method="post" {{ form_enctype(form) }} onsubmit="addIngredient();return false;">
   {{ form_errors(form) }}

<div>
    {{ form_errors(form.name) }}
    {{ form_widget(form.name) }}


<a href="#" id="add_ingredient"><img src="{{ asset('bundles/cookbookcore/images/plus.gif') }}" border="0" style="margin-bottom: -7px;" /></a>
</div>
        {{ form_rest(form) }}
    </form>